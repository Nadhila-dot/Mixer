# Mixer — Fixes Before May 20

Pitch day is May 20. The build is strong, the framing is hiding it. Fix the framing.

## The core problem
Submission tile reads as "another agent runtime." Actual demo is "Sentry alert → merged PR in ~90 seconds." Lead with the demo, not the architecture.

---

## Submission page (do first)

- [ ] **Rewrite the one-liner.** Kill "agentic runtime on the web." Try one of these:
  - *"From Sentry alert to merged PR in 94 seconds."*
  - *"The AI ops engineer that fixes your prod incidents and opens the PR."*
  - *"Give Mixer a stack trace, get a deploy."*
- [ ] **Swap the thumbnail.** Use the green-checkmark moment, or a split-screen of error-in / PR-out. Not the generic workspace shot.
- [ ] **Tags / track selection.** Make sure submitted to: Enterprise Utility, Agentic Workflows, Vultr Award. Add Multimodal if the demo touches a screenshot/PDF anywhere.

## Pitch structure (90 sec live demo, then ~60 sec context)

- [ ] **Cold open on the Sentry alert.** No intro slide, no "hi we're Calabasas." Alert hits the screen, agent starts working.
- [ ] **Show the messy middle.** Split-screen: reasoning on left, live terminal on right. Don't cut.
- [ ] **Stage one recovery moment.** Wrong path, failed test, missing dep — let Mixer self-correct on stage. This is the clip that gets shared.
- [ ] **End on the green check + merged PR.** Hold the frame for 2 seconds before talking again.
- [ ] **Then explain the moat (15 sec).** "Every run is a fresh Vultr VM. Agent gets root. We tear it down on merge."

## Vultr Award framing

- [ ] Write and memorize this line: *"Atlas visualizes Vultr. Mixer is Vultr, animated by an agent. Every session spins up a real VM, runs root-level, tears down on completion."*
- [ ] Make sure the demo actually shows a VM being spun up (timestamp it on screen if possible).
- [ ] If currently using one persistent VM, switch to per-session ephemeral VMs before pitch day. This is the whole Vultr story.

## Pre-load the "isn't this just X?" answer

- [ ] Rehearse this verbatim: *"Claude Code and Devin are dev tools. Mixer is the runtime underneath — what you'd build those on if you started today."*
- [ ] Five seconds. Move on. Don't argue.

## Final slide

- [ ] Live URL + QR code, big, centered.
- [ ] Demo creds if it's gated.
- [ ] Nothing else on the slide. No team photo, no thank you, no roadmap.

## Stretch (only if time)

- [ ] Record a 30-sec loop of the demo for the submission page (autoplay, muted, captioned).
- [ ] Have a second demo loaded as backup in case the first one wifi-fails on stage. Onboarding-a-new-hire or contract-audit are the easiest backups.
- [ ] One-line in submission description naming the Gemini / Claude / whatever model driving the loop — picks up the model-partner track.

---

## Don't bother with

- Adding more features.
- Rewriting the architecture diagram.
- Making the UI prettier.
- Writing a longer README.

The build is done. This is a marketing problem now.

---

**Good luck on exams. Ship this when you're back.**


api:JVO6M5S2HYWD42NH63HHSW6L3BT3XWT2APFA



ip 155.138.204.244
usrname root
and password e8.MC}EmnsuBJwyn

access this machine and check if docker is installed.
